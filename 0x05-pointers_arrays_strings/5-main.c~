#include "main.h"

int main(void)
{
char s[10] = "My School";

_putchar('[');
for (int i = 0; i < 10; i++)
{
_putchar(s[i]);
}
_putchar(']');
_putchar('\n');

rev_string(s);

_putchar('[');
for (int i = 0; i < 10; i++)
{
_putchar(s[i]);
}
_putchar(']');
_putchar('\n');

return (0);
}
